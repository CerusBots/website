apiVersion: v1
kind: Service
metadata:
  name: cerus-website
  namespace: cerusbots
spec:
  selector:
    app: cerus-website
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP